
<h1>Listing albums</h1>
 
<table>
  <tr>
    <th>Title</th>
    <th>Artist</th>
    <th>Released</th>
    <th></th>
  </tr>
 
  <% @albums.each do |album| %>
    <tr>
      <td><%= album.title %></td>
      <td><%= album.artist %></td>
      <td><%= album.released %></td>
      <td><%= link_to 'Show', album_path(album) %></td>
    </tr>
  <% end %>
</table>